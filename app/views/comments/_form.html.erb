<%= form_with(model: @comment, local: true) do |form| %>

  <div class="field">
    <%= form.label :Comment %>
    <%= form.text_field :content,class: "form-control" %>
  </div>

<div class="actions">
    <%= form.submit %>
  </div>
<% end %>
